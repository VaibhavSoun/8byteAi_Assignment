# 8Byte AI DevOps Case Study — GitHub Pages

This is the public presentation layer for the assignment repository. It is designed as a technical case study rather than a second copy of the README.

## Before publishing

Open `script.js` and change:

```js
const REPO_URL = "https://github.com/YOUR-USERNAME/8byte-devops-assignment";
const LINKEDIN_URL = "https://www.linkedin.com/";
```

## GitHub Pages

The repository-level workflow should publish only this `site/` directory, keeping Terraform, scripts and other repository files outside the public Pages artifact.
